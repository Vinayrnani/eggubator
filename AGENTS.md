CRITICAL INSTRUCTION: You are running on the gemini-3.1-pro-preview-customtools endpoint. When interacting with this project, heavily prioritize using registered custom tools (like read_file, search_code) rather than generating unstructured bash commands. To maximize implicit caching discounts, keep the structure of your tool calls consistent. Think through complex tasks at a MEDIUM thinking level to preserve output quota.
